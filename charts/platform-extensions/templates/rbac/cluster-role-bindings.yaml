{{- if .Values.clusterRoleBindings }}
{{- range $name, $binding := .Values.clusterRoleBindings }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ $name }}
  labels:
    {{- include "platform-extensions.labels" $ | nindent 4 }}
  {{- with $binding.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
subjects:
{{- range $binding.subjects }}
- kind: {{ .kind }}
  name: {{ .name }}
  {{- if .namespace }}
  namespace: {{ .namespace }}
  {{- end }}
  {{- if .apiGroup }}
  apiGroup: {{ .apiGroup }}
  {{- end }}
{{- end }}
roleRef:
  kind: {{ $binding.roleRef.kind | default "ClusterRole" }}
  name: {{ $binding.roleRef.name }}
  apiGroup: {{ $binding.roleRef.apiGroup | default "rbac.authorization.k8s.io" }}
{{- end }}
{{- end }}
